# Best-effort AUR packages, installed via yay as the regular user during
# the automated install (only if network is up). Failures here are
# LOGGED, not fatal - the install continues and finishes a working system
# either way.

visual-studio-code-bin

# broadcom-wl-dkms (this Air's wifi driver) is NOT here - it's an official
# package now (see packages.txt), installed unconditionally offline, no
# network/AUR needed. See README "Wifi reliability" for the history.
